<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        Add a "Stages" tab to the project settings form.

        project.edit_project is the settings dialog that opens when you
        click the three-dot menu → Settings on a project card, or when
        you create a project.  It is stable across Odoo 17-19.

        The Many2many field uses no_create / no_create_edit so users can
        only pick from the stage library managed in
        Configuration → Project Stages.
    -->
    <record id="view_project_form_stages_tab" model="ir.ui.view">
        <field name="name">project.project.form.stages.tab</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Stages" name="task_stages" icon="fa-columns">

                    <p class="fst-italic text-muted mb-3">
                        Select which stages are available for tasks in this
                        project.  To create or rename stages, go to
                        <strong>Configuration → Project Stages</strong>.
                    </p>

                    <field name="type_ids"
                           widget="many2many_tags"
                           options="{'no_create_edit': True, 'no_create': True}"
                           string="Active Stages"
                           nolabel="1"
                           domain="[]"/>

                </page>
            </xpath>
        </field>
    </record>

</odoo>